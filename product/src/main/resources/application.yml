spring:
  profiles:
    default: local
  data:
    mongodb:
      auto-index-creation: true

logging:
  level:
    root: INFO
    com.micro: DEBUG
    com.playground: DEBUG

---
spring:
  config:
    activate:
      on-profile: local
  mongodb:
    embedded:
      version: 3.0.0
  jpa:
    show-sql: true
server:
  port: 7001


---
spring:
  config:
    activate:
      on-profile: docker
server:
  port: 8080